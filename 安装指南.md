# 安装指南

## 前置要求

1. **微信开发者工具**：必须安装微信开发者工具才能运行小程序
2. **微信账号**：用于登录开发者工具（可以使用测试号）

## 安装步骤

### 第一步：下载微信开发者工具

1. 访问微信官方开发者工具下载页面：
   - 官网：https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html
   - 或搜索"微信开发者工具"下载

2. 根据你的操作系统选择对应版本：
   - Windows：下载 `.exe` 安装包
   - macOS：下载 `.dmg` 安装包
   - Linux：下载 `.AppImage` 文件

3. 安装微信开发者工具（按安装向导操作即可）

### 第二步：打开项目

1. **启动微信开发者工具**
   - 打开安装好的微信开发者工具

2. **导入项目**
   - 点击"+" 或 "导入项目" 按钮
   - 选择项目目录：`D:\workspace\github\forgetting-curve`
   - 填写项目信息：
     - **项目名称**：艾宾浩斯单词背诵（可自定义）
     - **AppID**：
       - 如果有小程序 AppID，填写你的 AppID
       - 如果没有，选择"测试号"或"不使用云服务"
     - **开发模式**：小程序

3. **点击"确定"或"导入"**

### 第三步：配置项目（可选）

#### 如果没有 tabBar 图标

如果项目中没有 `images` 文件夹或图标文件，tabBar 可能无法正常显示。有两种解决方案：

**方案一：移除图标配置（推荐）**

修改 `app.json` 文件，将 tabBar 配置改为只使用文字：

```json
"tabBar": {
  "color": "#7A7E83",
  "selectedColor": "#4A90E2",
  "borderStyle": "black",
  "backgroundColor": "#ffffff",
  "list": [
    {
      "pagePath": "pages/index/index",
      "text": "今日背诵"
    },
    {
      "pagePath": "pages/add-word/add-word",
      "text": "添加单词"
    }
  ]
}
```

**方案二：添加图标文件**

1. 在项目根目录创建 `images` 文件夹
2. 添加以下图标文件（建议尺寸 81px × 81px）：
   - `home.png` - 首页图标（未选中）
   - `home-active.png` - 首页图标（选中）
   - `add.png` - 添加图标（未选中）
   - `add-active.png` - 添加图标（选中）
   - `empty.png` - 空状态图标（可选）

### 第四步：运行项目

1. **编译项目**
   - 在微信开发者工具中，点击工具栏的"编译"按钮
   - 或使用快捷键 `Ctrl + B` (Windows) / `Cmd + B` (Mac)

2. **查看效果**
   - 左侧是代码编辑器
   - 中间是模拟器（显示小程序运行效果）
   - 右侧是调试工具

3. **开始使用**
   - 在模拟器中点击"添加单词"标签页
   - 添加你的第一个单词
   - 返回"今日背诵"查看今天需要复习的单词

## 常见问题

### Q1: 提示"找不到 app.json"？

**解决方案**：
- 确认项目目录选择正确
- 确认 `app.json` 文件在项目根目录

### Q2: tabBar 图标显示异常？

**解决方案**：
- 检查 `images` 文件夹是否存在
- 检查图标文件是否存在
- 或按照"第三步"移除图标配置

### Q3: 编译报错？

**解决方案**：
1. 查看控制台的错误信息
2. 检查文件路径是否正确
3. 确认所有必需文件都已创建

### Q4: 如何真机预览？

**解决方案**：
1. 点击工具栏的"预览"按钮
2. 用微信扫描生成的二维码
3. 在手机上查看小程序效果

### Q5: 数据保存在哪里？

**答案**：
- 数据保存在微信小程序的本地存储中
- 卸载小程序会清除所有数据
- 建议定期备份重要数据

## 项目结构说明

```
forgetting-curve/
├── app.js              # 小程序入口
├── app.json            # 小程序配置
├── app.wxss            # 全局样式
├── pages/              # 页面目录
│   ├── index/          # 主页面
│   └── add-word/       # 添加单词页面
├── utils/              # 工具函数
│   ├── ebbinghaus.js   # 艾宾浩斯算法
│   └── storage.js      # 存储工具
└── images/             # 图片资源（需要自行添加）
```

## 下一步

安装完成后，可以：
1. 阅读 `README.md` 了解项目功能
2. 阅读 `QUICKSTART.md` 快速开始使用
3. 开始添加单词，体验艾宾浩斯遗忘曲线算法

## 技术支持

如果遇到问题：
1. 检查微信开发者工具版本（建议使用最新版本）
2. 查看控制台的错误信息
3. 确认项目文件是否完整

