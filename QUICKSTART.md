# 快速开始指南

## 第一步：打开项目

1. 下载并安装 [微信开发者工具](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html)
2. 打开微信开发者工具，选择"导入项目"
3. 选择本项目目录
4. 如果没有 AppID，选择"测试号"即可

## 第二步：配置项目（可选）

### 如果没有图片资源

如果暂时没有 tabBar 图标，可以修改 `app.json`，将 `tabBar` 配置中的 `iconPath` 和 `selectedIconPath` 移除，只保留文字：

```json
"tabBar": {
  "color": "#7A7E83",
  "selectedColor": "#4A90E2",
  "borderStyle": "black",
  "backgroundColor": "#ffffff",
  "list": [
    {
      "pagePath": "pages/index/index",
      "text": "今日背诵"
    },
    {
      "pagePath": "pages/add-word/add-word",
      "text": "添加单词"
    }
  ]
}
```

或者完全移除 `tabBar` 配置，使用页面导航。

### 配置 AppID

在 `project.config.json` 中修改 `appid` 为你的小程序 AppID（如果有的话）。

## 第三步：开始使用

1. 点击"编译"按钮，小程序会在模拟器中运行
2. 进入"添加单词"页面，添加你的第一个单词
3. 选择开始背诵的日期
4. 返回"今日背诵"页面，查看今天需要复习的单词

## 功能说明

### 添加单词
- 输入单词和释义
- 选择开始背诵的日期（默认为今天）
- 点击"添加单词"保存

### 背诵单词
- 在"今日背诵"页面查看今天需要复习的单词
- 复习完成后点击"已复习"按钮
- 系统会根据艾宾浩斯遗忘曲线自动安排下一次复习时间

### 复习间隔
- 第1次：1天后
- 第2次：2天后（累计3天）
- 第3次：4天后（累计7天）
- 第4次：7天后（累计14天）
- 第5次：15天后（累计29天）
- 第6次：30天后（累计59天）
- 之后：每30天一次

## 常见问题

**Q: 为什么今天没有显示单词？**
A: 检查单词的开始日期是否设置为今天或更早的日期。

**Q: 可以修改复习间隔吗？**
A: 可以，修改 `utils/ebbinghaus.js` 中的 `REVIEW_INTERVALS` 数组。

**Q: 数据会丢失吗？**
A: 数据保存在本地，卸载小程序会清除数据。建议定期备份或导出数据。

**Q: 支持导入/导出吗？**
A: 当前版本不支持，可以在后续版本中添加此功能。

